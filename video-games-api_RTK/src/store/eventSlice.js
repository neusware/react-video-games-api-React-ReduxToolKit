import { createSlice } from "@reduxjs/toolkit"

const eventsSlice = createSlice({
  name: "events",
  initialState: {
    // We'll add the initial state for events later
  },
  reducers: {
    // We'll add reducers for events later
  },
})

export default eventsSlice.reducer

